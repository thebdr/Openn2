# Pipeline3App runtime dependencies
openpyxl>=3.1        # Excel read/write (strikethrough, formulas)
ruamel.yaml>=0.17    # comment-preserving YAML round-trip for project_params.yaml
tksheet>=7.0         # GUI grid (Files tab / config explorer)
pywin32>=306 ; sys_platform == "win32"   # Excel-cell links via COM (GUI)
sv-ttk>=2.6          # modern Win11-style (Sun Valley) dark ttk theme (GUI)
