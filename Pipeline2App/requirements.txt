# DatabaseTool dependencies.  Install:  pip install -r requirements.txt

# Core pipeline (run.py + safetydb/): read/write the Excel and CSV artifacts.
openpyxl
ruamel.yaml    # config/params.yaml loader; round-trip preserves comments on GUI save

# GUI (gui.py + editor.py):
tksheet        # spreadsheet grid in the Files editor tab
pywin32        # click-a-log-link -> jump to that Excel cell (optional; without it
               # the GUI falls back to just opening the workbook via os.startfile)

# Build only (not a runtime dep): package the designer tool into IOListChecker.exe
# via build_designer.bat / gui_designer.spec.
# pyinstaller
