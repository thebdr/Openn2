# C-025 (promoted from P-012) - Template builder: evidence

## Spec review (2026-09-25) - the user's decisions
- Template editing happens INSIDE the Files tab (a template mode of the text editor), not in a dedicated window.
- Literal braces are written DOUBLED (`{{ }}`, Python's pairing), so a TIA pragma is writable. Landed in C-024 as
  phase A (2c1e755); refute round 17 HOLDS.
- ROW templates are fully editable too (their values are lint-checked and previewed like text lines).
- The shipped builtin templates.yaml is READ-ONLY. "Create project copy" writes
  `<project>/config_project/systems/<sid>/chain_reactions/templates.yaml` (tier 1 of the resolver), and is disabled
  when no project is open.

## Phase B (2026-09-25) - the Tk-free core
- `tempemplator.lint(templates, start=, fields=, tables=)` walks EVERY branch of every text template, evaluating
  nothing. It uses the renderer's own checks through shared helpers (`_use_problem`, `_column_problem`,
  `_end_value`, `_check_directive`, `_scan`), so there is one message per problem for render and lint.
  - A rule's context (`start` + `fields` + the hook's `tables`) adds the strict missing field, the unknown loop
    table and the loop-column check, carried into @use'd templates.
  - Every template is also walked without context, because another rule at another hook may use it.
  - Predicates stay lenient: an unknown name there is a WARNING. So are a `where` reading its own loop var
    (round 16's note), the empty hole `{}` and a braced literal `{{$x}}` (round 17's note 1).
- `lint_line` checks one line / row value: lone braces, hole syntax, missing fields, loop columns.
- `engine.lint(templates, rule, database, hooks)` judges every entry for its KIND (text / row shape + values /
  neither). With a rule it adds the rule's E1 scope and the rule's own problems (template=None): the wrong kind,
  a table absent at the hook, a hook never fired, a target that will not render. An absent source table does
  not flood every hole as missing.
- `engine.preview(rule, row_index, ...)` is ONE fire for one source row through the fire path's own steps
  (`_template_problem`, `_scope`, the condition, `_spawned` / `_file_output`, carved out of `_spawn_rows` /
  `_append_file` with semantics unchanged: a spec is still added before the next renders). Rows are stamped
  as `add_row` stores them (`Table.stamped`, the uid included).
  - test_template_builder compares previews with REAL fires: the same rows, the same file text + path, the
    same finding (type + detail).
- `System.reaction_hooks` maps each hook to the Database it sees, declared by the run-plan. For Siemens:
  before_300 -> None; after_300 -> staging's `staged_database` (declared once, used by `stage_iolist`) +
  validation_issues.
  - The smoke pins the declaration against the real fire: the same hooks in order, the same tables + columns.
  - That pin CAUGHT a first draft that left out validation_issues: the 300/320 legs attach it before
    after_300 fires. The 310-only leg does not - noted in the loader's docstring.
- `workbench/template_doc.py` is the document model:
  - It parses with the engine's loader (a YAML error = rx_templates_unreadable, located).
  - Positions come from a round-trip parse's marks: literal blocks exact, folded / escaped quoted approximate.
  - Two-layer spans: YAML outside the bodies, SCL (FileXY langs.json) under them, and the template layer read
    through `tempemplator.outline` / `brace_runs` / `hole_parts` - the display never re-derives the grammar.
  - `place` maps lint problems to document offsets; `completions` works at the cursor.

Tests:
- test_template_builder (10): lint finds every one of 25 renderer errors on the renderer's own line, plus the
  untaken branch; the context lint matches the strict render; preview == fire.
- test_template_doc (5): positions exact on the shipped authoring manual; highlight tags on their constructs;
  problems placed on the culprit's text; completions from the context only.

## Refute round 1 (snapshot 775f3b7) - REFUTED; the implications check the same day

The refuter's findings, and what was done about each:

1. **HIGH: shipped config writable in the CSV grid and the Object explorer.** Self-found and fixed in
   abfd0c7 while the round ran. Pinned with a data-row shipped CSV (gate_rules.csv, bytes unchanged) and the
   Object-explorer preference.
2. **HIGH: format specs never linted.** `{$name:03D}` in an untaken branch linted clean, previewed fine, and
   failed the fire the day a motor row came. `{:03d}` linted clean too. Now:
   - `_spec_problem` flags a spec NO value can satisfy, tried with the kinds `format_spec` coerces to;
   - an empty expression before a spec is an error;
   - both are in `_BROKEN`, plus an untaken-branch case against a real fire.
3. **HIGH: the declared after_300 Database was the Database/ folder**, the last SAVED state. That meant a
   previous run's spawns and their columns, later phases' write-backs, and no settled record. It approved
   `{$source_uid}`, which the fire rejects, and rejected `@for chain_reactions_log`, which the fire accepts.
   The implications check found the same (post-500 values that the after_300 fire never sees).
   - Classified a BUG, not ambiguous_contract: the preview's promise is the fire.
   - `System.reaction_hooks` loaders now rebuild the fire's input IN MEMORY (Siemens after_300 = `stage(save=False)`).
   - The Session models `settle` (`engine.has_business` + `settle_view`) and the cascade.
   - Pinned against the REAL run-plan in `test_the_builder_models_what_the_after_300_fire_gets`: on a second
     run after spawns were saved, with a before_300 rule settled in, the tables, columns and rows are equal.
   - Staging is 0.7 s on the fixture; a big project builds in the view's worker thread.
4. **MEDIUM-HIGH: no in-hook cascade in the preview.** Now `engine.cascade` runs the earlier add_rows rules
   through the fire's own per-rule step `_act` (extracted from fire) on a copy. The preview of a later rule
   equals the real multi-rule fire (pinned).
5. **MEDIUM: the preview skipped the fire's guards.** Now:
   - `preview(hooks=)` gives rx_unfired_hook;
   - the per-rule backstop gives rx_rule_crashed (and the view's refresh is guarded);
   - `matched` is None when the fire stops BEFORE the condition, so "does NOT match" is only said when the
     condition says so;
   - the `self` field (a fire bug - see C-024).
6. **MEDIUM: typed YAML keys crashed the view.** Entries now keep text names and the raw values, and `place`
   matches `str(template)`. Pinned: `1:` and `123:`.
7. **MEDIUM: `|2-` shifted every line.** The block indent is now read off the data. Pinned.
8. **LOW-MEDIUM: escaped quoted values and `|`/`|+` overshoot.**
   - A single-line quoted scalar gets a per-character map (YAML's escapes decoded).
   - A body's range ends at its last non-blank line.
   - Pinned.
9. **LOW-MEDIUM: completions out of scope.**
   - A `where` offers only its loop table's columns.
   - A rule's fields are offered only in the templates the rule renders (`reach`).
   - Pinned.
10. **LOW: remaining lint gaps.**
    - An @if reading a loop row's missing column now WARNS.
    - A literal ':' in a file target is a rule problem.
    - The @use depth is judged order-independently (`_deep_chains`).
    - All pinned.

Pilot notes:
- A document a fire does not use now says so.
- The Object-explorer trap is closed (1).
- The preview's rows are now the fire's (3).

The implications check (C-025 vs the contract):
1. The saved state vs the fire's input: the same as refuter item 3, fixed there.
2. "Create project copy" vs C-004's full scaffolding. Every project the app creates already has a tier-1
   copy of all 16 shipped files. This is not a code bug; the clause and the guide now say so.
   - The shipped section is a read-only REFERENCE.
   - 'Open project copy' goes to the copy that runs.
   - 'Create project copy' is for a project without one.
   - The generalisation beyond templates.yaml is kept: the section is new, so nothing that was editable
     became read-only.
3. SCL was baked into the App. Now `System.template_language` supplies it (Siemens "scl"); none = no
   language layer (pinned).
4. The C-019 merge was dropped silently. It is now stated in C-025: "Extends [[C-019]] - merge when C-019
   re-verifies on PL5". The ƒx builder adopts the shared completion popup (one popup).
5. `reaction_hooks` is in no System / run-plan clause. It is stated in C-025 ("adds two fields to the C-021
   System contract") and in P-013's duties.
   - C-021 and C-023 (verified; behaviour unchanged) are left as they are; C-023's linked smoke re-records.
   - The builder notes a system that declares no hooks.
6. Stale guide lines in files-tab.md: conformed.
7. Stale docstrings (files_panel, files_view, main.py, iolist.py): conformed.
8. Tier 4, the builtin project config, is editable. This is stated in the guide: with no project open it
   IS the configuration that runs.
9. Spawns into a staged table do not reach generation (C-024's known boundary). The preview now notes it.
10. The lint's block walker duplicates `_scan_block`. The clause now says so ("a sibling walker, pinned
    against the renderer: each of its errors found on its own line"). A shared walker would mean reworking
    the verified renderer; not taken.
11. Minor:
    - "exactly" was qualified (folded / multi-line quoted scalars are approximate);
    - C-024's status value was normalised;
    - the P-001 evidence link was updated.

## C-024 refute round 19's note (a C-025 lint false positive) - fixed

`_literal_colon` exempted only a literal drive letter. A target like `{$_params.drv}:/out/{$name}.txt` got
the lint error "the fire refuses every row", yet the fire writes it when the hole renders a drive letter:
the builder rejected what the fire accepts. A ':' right after a LEADING hole and followed by a separator
is now the fire's to judge. Pinned with a real fire into a temp dir, the drive taken from its own path;
a hole's colon WITHOUT a separator is still flagged.

## Refute round 2 (snapshot 3622e07) - REFUTED: 9 findings, all fixed + pinned

The builder still approved things a real fire rejects, and rejected things it accepts. Each finding was
reproduced on the frozen snapshot (the refuter's probes, driving the real run-plan in the smoke's sandbox).

1. **MEDIUM: the settle was "attach the on-disk record"**, not what the fire gets. The real `settle`
   REPLACES before_300's audit rows with this run's and records its findings; the model loaded the old
   record (run 1: 0 audit rows vs the fire's 2). The pin built its model INSIDE the real fire, after the
   real settle - it compared the fire with its own output. Now:
   - the Session fires each pending database-less hook DRY (the real `fire` with `write=False`: every step
     but a file rule's write) and applies its Deferred through `engine.settle_view(database, deferred)` = `_persist(...,
     save=False)` - exactly the settle, minus the write - into a COPY of the built Database;
   - the smoke builds the model BEFORE each of two runs (a failing before_300 rule and a rule-index
     problem among the rules) and compares EVERY table's rows, in order, with the real after_300 fire's
     input - the audit and validation_issues included - and the preview of a rule listing them with the
     text the fire appends (`test_the_builder_models_what_the_after_300_fire_gets`).
2. **MEDIUM: the 310 leg was not modelled.** The 310 button fires after_300 over the I/O List alone (no
   C&E, no validation_issues). Now `System.reaction_legs` declares the other legs (Siemens: the 310
   Stage-I/O-List leg, `stage_iolist(save=False)`); every rule is linted against each - a problem only a leg
   has reads "on the 310 Stage I/O List leg: ...", one every leg has is reported once - and the preview
   notes the leg. Pinned against the real 310 fire's rx_bad_template.
3. **MEDIUM: the halt used the RAW severity**, the run-plan the severity after the treatments registry.
   Now the loaders go through `_gated`: `treatments.apply(findings, treatments.load())` + `should_halt`
   (read-only - the real gate also reconciles the registry; a preview must not write), raising
   `engine.WouldNotFire`. The builder then says the run halts before the hook - no rx_unknown_table errors
   of a fire that never runs. Pinned both ways against the real run: a FAIL treated 'warn' fires (no
   false errors; the preview = the fire's first line), a WARN treated 'fail' halts (one note; nothing
   written).
4. **LOW-MEDIUM: a literal Windows-invalid character in a file target** (`"rx/out.txt"`, `?`, `|`, `<>`,
   `*`) linted clean and previewed fine; the fire refused every row with EINVAL. Now the fire itself
   judges `<>"|?*` and control characters BEFORE the write, naming the character (C-024; the rows before
   it are written), and the lint's `_literal_refusal` flags a literal one. Pinned: lint = preview = fire
   for six targets; a character a ROW renders is data (no lint error; that row's preview = the fire's).
5. **LOW-MEDIUM: completions inside a data function's predicate were out of scope.** Now
   `count/where/first(<table>, ...` offers that table's columns (the innermost call's), and the lint WARNS
   on a column the table lacks - the predicate's `$col`, lookup's / unique's column words (a new parser
   helper `row_reads`) - in text lines, row values, `where` predicates and range ends. Pinned, with the
   fire's blank read (`count(signals, $cabinet_id = "C1")` = 0.0).
6. **LOW: unreadable project params.** The Session substituted {} and previewed a render; the fire BLOCKS
   the hook (rx_params_unreadable). Now the lint carries the fire's error, the preview says it, and the dry
   settle fires with the params unloaded (so the settled audit says rx_params_unreadable, as the run's).
   Pinned against the real fire.
7. **LOW: multi-line PLAIN scalars were misplaced but flagged exact** (YAML folds them). Now a plain scalar
   is exact only when its value sits verbatim in the document; an inexact one's problem is placed on its
   first line, labelled "(approximate)", and it gets no template colours. Pinned.
8. **LOW: the view.** The cascade ran on the Tk thread (0.5 s a keystroke at 20k rows), and a Reload
   during an in-flight build kept the stale build. Now:
   - `TemplateMode` runs EVERY Session call on one worker thread - a reload, builds, the settle, the
     cascade, the lint, the preview, the completion context; the Tk thread parses, paints and applies the
     newest result (one for an older request, or for a text edited since it was sent, is dropped); a check
     queued before a Reload is skipped; a rule change drops the old rule's completion context;
   - the Session numbers its builds: one a reload overtook is discarded;
   - pinned through the real FilesPanel: an instrumented Session (no call on the Tk thread; the Tk thread
     returns at once while a build blocks; the stale results dropped; each Reload on the worker, never
     during a build) and Tk-free (a reload during a blocked build).
9. **LOW: format specs ignored the value's type.** Now a spec on a JSON (list / object) value - the rule's
   own JSON field or a loop row's JSON column - WARNS (it fails on every non-blank row), and an untyped
   spec is also tried on a float (`{count(t):,.1}` renders). Pinned, with the real fire's rx_bad_template.

What held: grammar lint vs the renderer (0 mismatches in 48 adversarial directives); the cascade through
`_act`, `has_business`, the preview guards; the read-only shipped config in every viewer.

Self-found while pinning: the first dry fire sent its file rules to a scratch output root - an ABSOLUTE
target escaped it, so every view (a keystroke in a relevant template) appended to the real file. The dry
fire is now `fire(write=False)` - nothing written, wherever the target points; the audit counts the lines
a run writes (pinned Tk-free: an absolute target, nothing written; and in C-024 a dry fire = a real one's
Deferred).

Also self-found while pinning:
- **A halt on the main leg hid the 310 leg.** The injected C&E finding (the gate pin's) exists only in the
  C&E pass, so when it halts the full staging, the 310 button still fires after_300 - yet `check` returned
  at the main leg's halt. Legs halt separately now: the halt note, then each firing leg's own check; the
  preview notes the leg that fires all the same. Pinned in the gate test with a typo rule: the 300 run
  halts, the real 310 run rejects the typo, the builder says both.
- **A Database the loader cannot build** (the run's own staging would raise there) was linted as "no
  database" - rx_unknown_table errors of a fire that never happens (finding 3's pattern). It is a stop
  now, said like a halt. The round-1 pin that expected rx_unknown_table in that preview is updated.
- **`_data_table` ignored single-quoted strings** (the expr tokenizer accepts both quotes): a `)` inside
  `'a)b'` closed the call. Pinned.
- **The GUI result discipline was pinned by timing.** The mutation check found "apply every result" and
  "ignore an edit since" surviving (the debounce refresh raced the stale result). A deterministic pin
  now holds the worker inside each `check` until the test releases it: an older result arriving on its
  own is dropped, and so is the newest request's result for a text edited since it was sent.

Pilot tripwires the refuter named, now closed: a treated staging FAIL (3), the 310 button (2). Still true:
the shipped reactions.csv is empty, so real columns are offered only once the pilot writes a rule into the
project copy; "Open externally" can still edit a shipped file (outside the claim).

### The implications check of the round-2 change (zen-implications, fresh context)

Twelve items; what was done about each:
1. **The run-plan fired the reactions on a raw FAIL the operator DOWNGRADED** (every generation phase refuses
   one - main.py's `run.has_blocking` guards, gate.py "a buggy registry can't trick it into writing"). Worst
   case: a downgraded no-I/O-sheet FAIL let the after_300 fire save its EMPTY staged Database over
   signals.csv. Classified a BUG (C-024's run-plan): `run_staging` now skips the settle and the after_300
   fire on a raw FAIL (a WARN says so; the deferred before_300 firings are listed, not recorded), and the
   builder's loaders mirror it. The round-2 pin's "a FAIL treated 'warn' fires" direction flipped; an
   untreated WARN is the "fires" case now. Pinned through the real run-plan, the data-loss case included
   (`test_a_downgraded_no_io_sheet_never_saves_over_the_record`: signals.csv and the audit untouched).
2. **The builder's gate was a copy of the host gate's policy.** Now ONE decision: the kernel's
   `gate.halting` (read-only - `gate` computes the same `apply(load())` before its reconcile write), pinned
   equal to `gate` over every (severity, treatment) pair and writing nothing (test_phase_gate). The loaders
   add the raw-FAIL guard - the two kernel predicates the run-plan itself applies.
3. **A Session outlived a run and a project / system switch.** `FilesPanel.refresh` (after each run; on a
   switch via `set_sections`) now reloads an open builder - on its worker, discarding an in-flight build.
   Pinned in the GUI test.
4. **The dry fire's boundary.** A write only the OS refuses (a file held open, a permission) is counted as
   written - stated in C-024 and C-025.
5. **The plugin duties.** The System field comments now say each loader builds through its leg's own
   decision and raises WouldNotFire; P-013's duties cover the legs and the raw-FAIL guard, and its proposed
   verification pins each loader against the real run-plan.
6. **The 310 leg's validation_issues.** With a before_300 settle the record is attached from disk, so the
   310 leg HAS validation_issues - the builder already modelled it; the contract, guide and manual said
   otherwise. Reworded, and pinned both ways against the real 310 fire.
7. **"A C&E finding".** The duplicate (script_type, index) FAIL is the full staging's SECOND pass's, not a
   C&E problem - reworded (contract, guide, the test's comment).
8. **The cascade ran over params that do not load** (the fire blocks every rule: nothing spawns). Skipped
   now; pinned (a later rule counts no phantom spawns).
9. **treatments.md named a registry path that does not exist** - now
   `config_project/shared/user_input/finding_treatments.csv`, and the guide says a downgrade never
   generates; files-tab.md links it.
10. **The shipped manual** now says after_300 also fires on the 310 button (and what that Database lacks),
    and that no reaction runs on a raw FAIL.
11. **A stale engine comment** (a `"` as the OSError example - refused before the write now) - fixed.
12. **The leg label is an English literal** ("310 Stage I/O List") in System-contract data. Accepted under
    P-005 (i18n everywhere): the builder's UI is English-only today.

Wording: a stop is "the run stops before after_300 fires - <why>" (a halt, a downgraded raw FAIL, a
Database that cannot be built).

### Mutation check (scratchpad mutate_c025.py) - 95/95 KILLED

The 49 earlier mutants (7 re-anchored on the refactored code with the same intent) plus 46 for round 2's
fixes and the implications check. Three runs, each on a fresh copy of the tree:
- the first full run (85): W2 / W3 survived - "apply every result" and "ignore an edit since", because the GUI
  pin was timing-bound (the debounce refresh raced the stale result). The deterministic pin (the worker
  held inside each `check`) replaced it.
- the full run on the round-2 code (90): R21 survived - the "no match" note's round-1 pin had become a
  build-failure stop. Re-pinned: a source table missing from a Database that does build.
- after the implications fixes, 20 on the final code: R21, the new ones (the loader's `gate.halting` and
  raw-FAIL guard, `halting` reading the raw severity, the params cascade skip, the refresh reload, G1 and
  R20 re-anchored) and every mutant pinned by the Siemens smoke, against the rewritten gate test: 20/20.
