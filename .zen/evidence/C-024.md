# C-024 — Chain-reaction engine + Tempemplator text renderer (PL5 step 6, engine leg)

Promoted from P-009 on landing, 2026-07-17. The engine LANDS DARK: zero shipped rules, so the
byte-parity oracle proves "an empty rule set changes nothing" (39 output files, 0 diffs).

## What landed, and where (the blend homes)

- **`language/tempemplator.py`** — the recursive text renderer, named for the user's VBA tool
  (their FUH_DHL belt-code generator, studied 2026-07-16). Rebuilt on `language/expr` with the
  construct map 1:1 (the module docstring carries the full table). The original's guillemet/dollar
  split survives as @-directives (structure) vs {expr} holes (values). Holes render STRICT — a
  typo'd field is a located TempemplatorError(template, line), never a silent blank. `@if` branches
  are LAZY (scan-then-render-taken-only): a strict hole inside the untaken branch never evaluates —
  the guard pattern `@if present($x) … {$x.field}` works. The E3 upgrade the VBA could never do:
  `@for $row in <table> [where <pred>]` loops over SSOT rows.
- **`phases/chain_reactions/engine.py`** — the rule engine chapter. `compile_rules` validates each
  reactions.csv row (malformed → located ERRR, dropped); `fire(hook, database)` matches, spawns
  (`add_rows` with `spawned_by`+`source_uid` provenance) or appends (`file`, the ONLY mode — user
  decision), audits everything into `chain_reactions_log` (replaced per hook = the last run), records
  failure findings to validation_issues, saves. STRICT NO-OP on a rule-less hook.
- **Config**: Siemens ships `chain_reactions/reactions.csv` (header only — CSV has no comment
  syntax; an example row would compile as a live rule) + `templates.yaml` whose comments are the
  authoring manual and whose three entries are the VBA WORKED EXAMPLES (Ex.1 leaf line + loop var;
  Ex.2 structure template with counted loop + nested @use; Ex.3 the inline conditional as plain
  expr if()/present()/concat()) — executable reference, rendered for real by the test suite.
- **Hook wiring**: the SYSTEM RUN-PLAN (safety/main.py `run_staging`) fires `before_300` (no
  database yet — file rules only) and `after_300` (the freshly staged database). The P-009 draft
  said "kernel phase dispatch, no per-main code" — that pre-dated the blend: L2 forbids
  chapter-to-chapter calls and the step-5 architecture made the run-plan THE dispatch, so the
  hooks live there. One anchor for now; more anchors arrive with the config folds.

## Design decisions the draft left open (resolved here)

- **CSV columns**: `action` + `target` replace the draft's single `target_table` — the `file`
  action needs a PATH (expr-rendered, relative → under the project output root); the draft
  predated it. `source_table` empty = a FIRE-ONCE rule (e.g. a generated-file header).
- **Failure severity**: ERRR (skip the rule, continue, loud) per the severity taxonomy — FAIL
  would halt generation for a mis-authored optional rule; ERRR marks the output possibly
  incomplete without killing the run.
- **The log replaces per hook** (uid keys hook/rule/target): the Explorer shows the LAST run's
  audit, not an unbounded history; determinism preserved (no timestamps in the SSOT).

## Known boundary (deliberate)

An `after_300` `add_rows` spawn persists into the SAVED Database (Explorer-visible) but downstream
phases RE-STAGE from the source document — spawned `signals` rows do not yet flow into generation.
That absorption is the generated-signals FLAGSHIP, which the standing spec-review gate holds until
its own reviewed step. The engine's mechanics are complete and proven; the wiring depth grows there.

## Found in passing (fixed at the root)

The expr parser (`language/expr/parser.py`, the C-002 surface) raised a raw **IndexError** on a
truncated expression (`let(`, `concat(`, …) — `_Parser._next()` indexed past the token list, escaping
the "a bad reference is a located ExprError" contract. Found by the engine's bad-condition test;
fixed with the EOF guard; pinned at the expr level in
`test_expr_adversarial.test_truncated_expression_is_a_located_error`.

## Also in this step (separate commit): the M1–M6 documentation pass

The P-014 manual-grade standard applied to the 14 verbatim-moved Siemens toolchain modules
(docstrings/comments ONLY — +148/−6 lines; every related suite green; parity re-proven 0 diffs).
Each module now declares its place in the flow (M4: phase button → tables read → files written),
links its seams (M5 src://), and carries its decision history (M6: the OPC-lock, the byte-stability
rationale, the dated contract evolutions).

## Gate at landing

67/67 test files green (test_tempemplator 6/6 · test_chain_reactions 9/9 · the expr suites with the
new EOF pin) · `scripts/parity_vs_pl4.py` exit 0 (14 SSOT + 39 output files) · laws L1–L5 green,
ratchet still EMPTY (the new chapter imports only downward; the run-plan wiring is systems→phases).
Recorded run: `Pipeline5App/scripts/verify_c024.sh`.

## Refuter round 1 (2026-07-17): REFUTED — two verification-adequacy holes, both fixed

Behavior was correct; the EVIDENCE was not sufficient:
1. **The run-plan wiring was unasserted**: none of the four linked verifications touched
   `run_staging` — the parity oracle drives `staging.stage()` directly (structurally blind to the
   handler wiring), and the unit tests inject rules into `engine.fire` without importing main. A
   deleted/typo'd hook call survived the whole set. FIX: `test_siemens_main_handlers.py` gained
   `staging_fires_reaction_hooks` (a fire-recorder through the REAL dispatch + REAL loaders:
   before_300 with no database, then after_300 with the staged one, in order, no halt) and joined
   the verification links + verify_c024.sh.
2. **The laziness pin was weak**: the untaken-branch assertion used a PRESENT-but-empty field,
   which an eager renderer also passes. FIX: refuter-grade pins — an ABSENT-field hole, an
   unknown `@use`, and an unknown-table block `@for` inside the untaken branch.
3. (Minor, also fixed) `fire()` loaded templates/params even when the hook had no ACTIVE rules but
   compile findings existed; the fast path now returns before any further load, and the clause's
   "nothing loaded" wording is corrected to "beyond reading the rule index itself".

Dismissed by the refuter as non-clause (recorded as known hazards): a literal line starting with
`@end`/`@else` parses as a directive (SCL never does); a MIXED rules list (Rule + dict) raises an
unlocated error (outside the stated polymorphism).

## Refuter rounds 2-3 (2026-07-17): two more mutation-confirmed evidence holes, both fixed

- R2: the fast-path "compile findings still surface" guarantee was unasserted (`return database, []`
  survived). Pinned in test_fire_empty_hook_is_a_strict_noop: a hook-unparseable rule fired at a
  rule-less hook must propagate its rx_bad_rule finding while writing nothing. R3 re-killed the mutant.
- R3: the engine's PRODUCTION of the `_db` scope layer (`db_tables` from database.names()) was never
  exercised end-to-end (`db_tables = {}` survived all five oracles - the shipped rules are empty, so
  parity never builds it). Pinned in test_engine_builds_the_full_e1_scope: a file rule whose template
  cross-queries ANOTHER table via `@for $r in other` + drills `{$_params.project.name}` dotted,
  fired through the engine; the same test pins the audit row's `action`/`target` columns (the two
  enumerated columns nothing read).

## Refuter rounds 4-5 (2026-07-17): the last holes

- R4 (evidence): the PER-HOOK scope of the log replacement was unpinned (`log.rows = []` clobber-all
  survived) and `_rule.hook` was unasserted. Pinned: the two-hook survival case in
  test_rules_fire_in_csv_order_and_only_their_hook + `{$_rule.hook}` in the E1-scope template.
- R5 (a REAL BEHAVIORAL BUG, the only one of the five rounds): error line numbers inside @if/@for
  blocks were SLICE-RELATIVE, not template-absolute - `_render_block` re-indexed pre-sliced bodies
  from 0 with no offset, so a typo in a nested template (exactly the shape of the shipped worked
  examples) mislocated. Fixed by threading a `base` offset through the block/branch/inline slicing;
  pinned by test_errors_locate_true_template_lines (taken-@if line, @else-branch line, @for-block
  line, inline-@for attribution to the @for line, double nesting).

## Refuter round 6 (2026-09-24, fresh context, after the WIP split): REFUTED — 5 real bugs, 5 evidence holes

Run against `git diff 13bad07 b5c7ca8`, every mutation in a scratch copy (repo untouched), all
five bugs reproduced on UNMODIFIED code (probe_behavior re-run by the orchestrator: identical).
Key meta-finding: **parity_vs_pl4.py never imports the engine, the renderer or safety/main.py** —
the engine lands dark, so parity proves "no rules changes nothing" and NOTHING else; only the unit
files can catch engine defects.

What HOLDS: the full R5 revert fails; the parser EOF guard is pinned; inclusive ranges, lazy @if,
spawned_by/source_uid provenance and per-hook replacement-on-success are pinned; the cycle guard's
removal is backstopped by the depth-32 located error (message-only difference, not reported).

Real bugs (claim unmet on unmodified code):
- **B1 "never a crash"**: raw exceptions escape `fire()` — a templates.yaml plain scalar starting
  with `@` (ruamel ScannerError; `@` is YAML-reserved and the renderer's own directive sigil), a
  rendered file target with a Windows-invalid char (`"`/`|` → OSError 22 — a quoted Siemens tag),
  a bad regex in a condition / hole / `where` (re.PatternError — expr's re.compile unguarded), a
  row template that is not a list of mappings (ValueError / TypeError), `@for 1..{$n}` with n=inf
  (OverflowError), a TAB before `in` (ValueError). The engine catches only ExprError/TempemplatorError.
- **B2 "recorded to validation_issues"**: record()+save() run only when log rows exist — a rule
  failing at MATCH time (unknown source table, bad condition) renders its finding but never
  reaches validation_issues.csv; before_300 failures never do. The test's assertion passed only via
  a sibling finding.
- **B3 "REPLACED per hook"**: a re-fire whose only rule fails leaves the PREVIOUS run's success row
  in chain_reactions_log (the replacement is skipped when no rows were produced).
- **B4 "a malformed rule = a located ERRR"**: `load_reactions` silently drops a row with an empty
  `name` (the compile-time `<unnamed rule>` check is unreachable from config); conditions and
  template existence/kind are not checked at compile time (an empty source table hides `let(`).
- **B5 "STRICT holes"**: strictness covers top-level names only — `{$r.tagg}` in a table loop,
  `{$_params.project.nmae}`, `{$_rule.nmae}` render blank; every row-field typo in an E3 loop is silent.

Evidence holes (mutant survives every linked verification):
- **E1** no test fires a CONFIGURED rule through the real run-plan (the smoke hits the empty shipped
  CSV): dropping the loader, the output-root / params / templates defaults all survive.
- **E2** the strict no-op's "nothing further loads" is unpinned (moving the early return below the
  loads survives, and is NOT equivalent: it crashes staging on a broken templates.yaml with no rules).
- **E3** deleting both `ctx.render(rx)` calls in run_staging survives.
- **E4** the R5 fix is only partly pinned: dropping the offset in the @else branch, the @for block,
  the inline @for, or `_scan_block(base=0)` each survive with a wrong line (only taken-@if nesting
  is exercised).
- **E5** a non-rendered range START and a `where` predicate that sees outer fields both survive.

Verdict recorded: `refutes/C-024.json` = refuted. Status stays `pending verification`; next = fix
B1–B5 at the code, close E1–E5 with pins, re-refute.

## Round-6 fixes (2026-09-24) — zen-failure: classification `bug` (the clause was sound, the code diverged)

Root causes: (B1) the engine's error boundary was TYPE-based (ExprError/TempemplatorError), and
lower layers leaked non-contract exceptions; (B2/B3) persistence was gated on "produced log rows",
conflating *produced rows* with *fired*; (B4) the loader's blank-line filter keyed on `name`, and
compile deferred condition/template validation to match time; (B5) expr STRICT checked only the
top-level name. Fixes, in the blend homes:
- **expr (C-002 surface)**: `_compile_regex` — a malformed `/regex/` is a located ExprError at all
  three sites; STRICT render resolves every referenced dotted path fully (`referenced_paths` +
  `_resolves`); empty/keep modes and evaluation unchanged (silent-empty on a sub-key), pinned.
  Behavior-neutral for the other strict callers (datablock templates, interface SCL, OPC diag
  SCL): no shipped config hole references a dotted path — the parity oracle re-proves it.
- **tempemplator**: `_FOR_HEAD` (any whitespace around `in`; the var must be hole-referenceable);
  non-finite range ends are located errors; `_test` + a `_render_line` backstop make EVERY
  evaluation failure a located TempemplatorError.
- **engine**: per-rule backstop (`rx_rule_crashed`); `rx_file_write`; guarded config loads
  (`rx_rules_unreadable` / `rx_templates_unreadable` / `rx_params_unreadable` block the hook's rules,
  each audited); compile checks an empty name + COMPILES the condition (`expr.check`); the template's
  existence / kind / row shape (incl. a null field value) is checked before matching; the audit row
  gains `outcome` (`ok` or the finding type) and a hook's rows are replaced on EVERY fire that has
  rules (valid or malformed); findings are recorded uid-deduplicated (a malformed rule surfaces at
  every hook, lands once); a DATABASE-LESS hook returns a `Deferred` (audit rows + findings) that
  the run-plan `settle`s into the staged record (safety/main.py, between staging and after_300).
- **loader**: only fully blank lines are skipped — a nameless row reaches the compiler.
- **docs**: the engine chapter (finding-type catalog, audit, Deferred), the tempemplator/expr
  docstrings, and the templates.yaml authoring manual (a text template must be a block scalar —
  YAML reserves a leading `@`).

Design decisions made here (reversible, recorded): a FAILED rule gets an audit row carrying its
outcome (the refuter's "a failed rule still gets a row" — "every firing audited" read literally); a
null row-template field value is an authoring error, not a silent "None"/blank; the before_300
trail travels as an explicit `Deferred` (no module state — tests and the GUI never leak a stale
trail into another database).

Test pins (all green): test_chain_reactions 10→17, test_tempemplator 7→9 (+E4 nested offsets,
+E5 rendered start / row-only where scope), test_expr_adversarial 24→26, and the smoke's new
`configured_rules_fire_through_the_real_run_plan` — a PROJECT (tier 1) rule pair through the REAL
run_staging: exact probe.txt bytes (the before_300 header + the after_300 line drilling the real
project params, under the real output root), the four audit outcomes (before_300's SETTLED), both
failures in validation_issues.csv, both RENDERED.

**Mutation check (orchestrator, scratch copy, 2026-09-24): 32/32 killed, 0 survivors** — the
refuter's 14 round-6 survivors re-expressed on the new code (M0 R5-revert, M7–M10 nested offsets,
M11 unrendered start, M16 leaking where scope, M1/M13 rules never loaded, M2 wrong output root,
M3/M4 empty params/templates defaults, M5 fast-path disabled, M6 render calls dropped) + 18 against
each new guard (deep strict, regex guard, overflow, tab head, engine backstop, record, per-hook
replacement, dedupe, settle, compile-time condition, unnamed, template pre-validation, file-write
guard, config-load guard, Deferred, owned-findings-as-business, loader filter, run-plan settle).
Each killed by the pin written for it. Defense-in-depth observed: with the parser regex guard
removed, the renderer's own backstop still locates the error (the expr + engine pins catch it).

Known hazards, deliberately NOT changed (outside the clause, noted for the record): an enormous
range (`1..1e9`) materializes its list (no cap - a cap is a config decision); a relative target
with `..` may escape the output root (absolute targets are allowed by design anyway); removing
every rule leaves the previous run's chain_reactions_log.csv on disk (the strict no-op writes
nothing, by contract).

## Implications check after the round-6 fixes (2026-09-24) — the deep-strict fix SUPERSEDED

A zen-implications pass over the redefined clause found the B5 fix itself wrong, plus three gaps.
**This section supersedes the "deep STRICT" bullet and the N18 mutant of the section above.**
- **Deep strict REVERTED (C-002 wins).** Pre-scanning every dotted path against the current row
  broke C-002's optional-sub-key design: `coalesce($type.tristate,'0')` / `present(...)` guards
  raised; data-function predicates (whose `$col` means the ITERATED row) were checked against the
  current row; node rows (empty `type` cell) failed `{$r.type.type_id}`; a project-tier
  `{$type.<key>}` DB-member template would have turned a blank into a phase-520 FAIL (C-008 — the
  "behavior-neutral" claim only held for SHIPPED config). Classification: wrong_assumption — a text
  scan cannot tell a typo'd key from an absent optional one. B5's real complaint (row-field typos in
  E3 loops) is a SCHEMA question, now answered precisely: inside a `@for` TABLE loop, `$row.column`
  must name a column the table's rows carry (a range var has none); deeper JSON keys and `_params`
  keys stay optional (C-002 / the C-004 get_param precedent); predicates stay lenient (the guard
  idiom). expr keeps only the regex guard; its strict render is byte-identical to before.
- **Halted staging (gap).** Settling before the gate is WRONG: the no-I/O-sheet halt writes nothing
  (a settle would save an empty signals.csv over the previous run). Resolution: a halted staging
  commits no reaction record; the run-plan lists the deferred before_300 firings in the log (INFO,
  never hidden) — their findings were already rendered. Pinned by a halted-path smoke.
- **Unreadable reactions.csv (gap).** Rendered at both hooks, never recorded: now every hook's
  business (the before-hook defers it, the staged hook persists it, recorded once).
- **Unfired hooks (gap).** A rule on a hook nothing fires (`after_520`) compiled clean and waited
  forever: the run-plan now passes its `hooks`; any other hook is `rx_unfired_hook`. Rule-INDEX
  problems (a row naming no hook, an unfired hook, an unreadable file) are every hook's business —
  rendered at each fire, recorded once (the no-op fast path now requires a CLEAN index).
- **Docs/refs fixed**: the templates.yaml authoring manual (strict holes, optional sub-keys + how
  to guard, hooks), the tempemplator chapter (strictness section, `{$_params.project_code}`), the
  chain_reactions signpost (the run-plan duties: pass `hooks`, settle after the gate), the language/
  and phases/ signposts (no longer "arrives with P-009"), docs/guide/expressions.md (a Strict
  templates section), the dead `re.error` in datablocks/templates.py, the verify_c024.sh header.
- **Contract**: C-024 text re-sharpened (schema-strict, index problems, unfired hooks, the halt
  boundary; "Extends [[C-002]] - merge when C-002 re-verifies on PL5"); P-012 (the builder's lint
  and preview must match the consumers: strict preview + the loop-column check); P-013 (RTX
  run-plans take on the pass-`hooks` + settle duties).
- Checked clean by the pass: P-014 provenance, C-001 (an old 7-column log still loads), treatments
  (settled findings stay treatable), C-017.
- **Mutation check on the FINAL code (scratch copy): 40/40 killed** - the refuter's 14 round-6
  survivors (M0-M16), 17 round-6 guards (N19-N36; N18 retired with the deep strict), and 9
  follow-ups (S1-S5 the loop-column schema: removed / never set / not restored / range var unchecked /
  deep keys checked; U1/U3 unfired hooks; R1 unreadable file not business; H1 the halt listing).
  U3 first SURVIVED - a malformed rule on an undeclared hook was only recorded because the fired
  hook happened to have its own rule; the lone case is now pinned and U3 dies.
- Early gate on the working tree: the full 67-file suite green + parity 0 (the expr regex guard and
  the datablock handler cleanup left every strict emitter byte-identical).

## Refuter round 7 (2026-09-25, fresh context, at ca4fac7): REFUTED — 5 bugs, 3 evidence holes; fixed

Run on the committed tree (git archive) in scratch; every bug reproduced on unmodified code. Parity
cannot see any of them (it never imports the engine or main.py).
- **B1 (serious)** a condition like `extract($tag, /(\d+)/, 1.3)` (a decimal typed for the slice
  `1:3`) raised a RAW ValueError out of expr's `_slice` -> `expr.check` -> `_compile`, OUTSIDE the
  per-rule backstop: since the whole index compiles at every fire, EVERY hook crashed (run_staging
  before staging even ran). Fix: `_slice_bound` raises a located ExprError (C-002 surface); the
  compile guards the condition check (rx_bad_condition) AND each row (an unreadable row = an index
  problem named `<row n>`) - compiling never raises.
- **B2** the loop-column schema came from the ROWS; freshly staged in-memory `signals` rows lack 8
  declared columns (name_in_db, ...), so a real column failed at after_300 only (fine over the saved
  record). Fix: the engine's `_db` rows carry every column of their table (absent -> "", exactly how
  expr reads a missing key - data functions and predicates are unaffected).
- **B3 (data loss)** the 310 leg stages a Database without validation_issues: recording a reaction
  finding created an empty table and SAVED it over the file - wiping an earlier phase-110 record.
  Fix: `_attach` loads the on-disk record (and the audit) when the database never loaded it; the
  uid dedupe now runs against the real record.
- **B4** `created` was assigned only on a clean return (a mid-action crash audited 0); `{$n:03d}` on
  `inf` leaked a raw OverflowError (filed as a code crash). Fix: a tally counted as rows/lines are
  made; format_spec converts OverflowError to ExprError (C-002 surface) -> rx_bad_template.
- **B5** `_split_top` did not treat `/regex/` literals as opaque: `where $tag ~ /^P..1$/` split as a
  range, a `:` inside a regex as the inline body. Fix: regex literals (and escapes) are opaque, as in
  render's own hole splitter.
- **E1** nothing pinned that before_300 passes the run-plan's `hooks` (the merged render set hid it);
  **E2** nothing pinned that ANOTHER declared hook's malformed rule surfaces; **E3** the templates
  block was only tried with injected params (the default path's params load could clobber it), and
  rx_params_unreadable was unexercised. All pinned: the halted smoke now asserts the EXACT rendered
  set (the unfired hook + after_300's uncompilable rule surface at the only fire of a halted run);
  unit cases for the other-hook surfacing and both config-load paths.

Deliberately noted, not changed: after a dup type/index halt the previous run's
chain_reactions_log.csv sits beside this run's staged tables - a variant of the recorded stale-log
hazard (a halted phase commits no reaction record, by the clause).

New pins: test_chain_reactions 20->26 (compile never crashes, declared-column schema, append to the
on-disk record + audit, created survives both failure kinds, other-hook surfacing, both config-load
paths), test_tempemplator (regex-in-where, inline + block), test_expr_adversarial (decimal slice,
format overflow), smoke 5->6 (the configured project gains the decimal-slice rule and a loop over the
STAGED signals reading a declared-but-unfilled column; the halted smoke's exact render set; a 310-leg
smoke proving an earlier record survives).
Mutation check on the round-7 code (scratch copy): **49/49 killed, all applied** - the 40 earlier
mutants re-anchored + 9 against the round-7 guards (V1 slice bound, V12/V13 the compile guards
removed in layers, V4 format overflow, V5 `_db` not normalized, V6 record not loaded from disk,
V7a/V7b the created tally, V8 regex not opaque in the splitter).

## Refuter round 8 (2026-09-25, fresh context, at 25a7b3f): REFUTED — 5 bugs, 4 hole groups; fixed

- **R1** `_split_top` tracked quotes/regex/braces but not call ARGUMENTS: `where extract($tag, /x/,
  0:1) = "1"` split at the slice colon, `where let(n := ...)` at the `:=` (the block form misread as
  inline too) - the unfinished half of round 7's B5. Fix: `(`/`[` depth.
- **R2** only the `_db` rows were completed: a MATCHED row still lacked the columns staging leaves
  unfilled, so a top-level `{$name_in_db}` (text, row template, target path) failed on all 269
  staged rows while `{$r.name_in_db}` worked - round 7's B2 fresh-vs-saved mismatch, half-fixed.
  Fix: one `_complete_rows` feeds both.
- **R3** expr's keep/strict missing-field check scanned tokens: a data-function predicate's `$col`
  (the ITERATED row's) was demanded of the hole's own ctx - `{count(signals, $script_type = "PEC")}`
  failed in a fire-once template; let-bound names too. Fix (C-002 surface): `parser.free_fields` -
  the fields the PARSE reads from ctx (predicate sub-parsers collect privately; let bodies/bindings
  exclude bound names); render decides over it. Only false errors disappear: a template that rendered
  before renders identically (parity re-proves it).
- **R4** round 7's `_attach` read the on-disk record: a ragged hand-edited CSV crashed fire()/settle()
  (after staging saved and before_300 appended: no RSLT, the trail lost); a file held open (Excel's
  deny-write) crashed the save - that one since landing. Fix: `_persist` never raises -
  `rx_record_unreadable` (the record is left UNTOUCHED, nothing recorded over it) / `rx_record_unwritable`;
  `settle` returns them and the run-plan renders them.
- **R5 (latent)** `rx_rules_unreadable` took the firing hook's phase - one broken file = one record per
  PHASE once a run-plan's hooks span phases. Fix: file-level findings carry phase 0.
- **EH1-EH4** `_split_top`'s escape skip, brace depth and quote opacity were unpinned (each a
  surviving mutant); the within-fire dedupe (`known.add`); extras in `effective_columns`; the
  renderer's evaluation backstops (a 1500-deep `not` chain: RecursionError) - all pinned.

New pins: test_chain_reactions 26->31, test_tempemplator (slices / let / quotes / escapes / braces in
@for lines, the recursion backstops), test_expr_adversarial 26->27 (free fields), smoke 6->7 (the
configured project gains a top-level declared-column rule over the staged signals and a fire-once
count(); a corrupt audit CSV through the real run_staging: rendered TWICE - settle + after_300 -
the run completes, the file untouched).

Mutation check on the round-8 code (scratch copy): **65/65 killed, all applied** - the 49 earlier
(three re-anchored after round 8 moved their lines: N23 record, N26 settle, N32 the run-plan settle)
+ 16 new: W1 call-argument depth, W2/W3/W4 the free-field parse (predicate columns, let names, a
scanned set), W5/W6 the record load/save guards, W7 the settle findings rendered, W8 file-level
phase 0, X1-X4 the splitter escape/brace/quote opacity, X5 the within-fire dedupe, X6 extras in
the schema, X7/X8 the renderer backstops.

## Refuter round 9 (2026-09-25, fresh context, at 37531df): REFUTED — 1 medium bug + smaller ones; fixed

A differential fuzz of `free_fields` against the evaluator (6000 expressions) found 0 mismatches -
the round-8 parse is sound. The finds:
- **(medium) the cascade within one hook** - `_db` was snapshotted ONCE per fire while matching read
  the live table: after the manual's own example spawned 24 DIAG signals, the next rule counted
  `0.0` and listed none (every rule audited `ok`, nothing rendered - a silent wrong answer in the
  engine's core use); a spawned column even failed the loop schema. Fix: `_db` rebuilt after a rule
  spawns rows. Pinned by a two-rule unit cascade and through the real run_staging.
- **spawns lost beside a ragged record** - `_persist` returned before the save: the after_300 spawns
  never reached signals.csv while RSLT reported them. Fix: the record tables attach from disk BOTH
  or NEITHER (a half-attached table would be saved), and the rest of the Database is always saved.
- **the loop-column check still token-scanned** - a predicate's `$type.type_id` or a let-bound `$r`
  sharing the loop var's name raised false errors. Fix: it reads the PARSED free paths
  (`expr.hole_paths` over `parser.free_paths`).
- **row-template values coerced** - a YAML list stored as its repr, `007` as `7`, `true` as `True`.
  Fix: every value must be a template string (rx_bad_template otherwise).
- **holes**: a ragged validation_issues.csv (only the audit CSV had been corrupted in tests); a
  condition failing at EVALUATION (not compile) must stay rx_bad_condition - both pinned.
- **trivial, fixed anyway**: range ends must be WHOLE numbers (`1...3` rendered nothing, `2.7`
  truncated) and an un-iterable range (`1..1e308`) is located; keep mode falls back to the token
  scan for a malformed hole (its exact pre-round-8 behavior - identity's two-stage tagnames).
Tests: chain_reactions 31->34, tempemplator (collisions, ranges), expr_adversarial (None + keep
fallback), smoke 7->9 (the cascade through run_staging; spawns saved beside a ragged record, the
RSLT count matching the saved file).
Mutation check on the round-9 code (scratch copy): **72/72 killed, all applied** - the 65 earlier
(M11/N20/N23/S1/V6/W3/W4/W5/W6/X7 re-anchored after round 9 moved their lines) + 7 new: Y1 the
cascade rebuild, Y2 the record attached one table at a time, Y3 an unreadable record skipping the
whole save, Y4 row values coerced, Y5 the loop check back to a token scan, Y6 the range-building
guard, Y7 keep mode without its malformed-hole fallback.

## Refuter round 10 (2026-09-25, fresh context, at ac2d8c0): REFUTED — 1 real bug (a DECISION), 3 holes pinned

- **OPEN - needs the user's decision:** a BLANK range end is silently read as 0 (`... or "0"` in
  `_iterate`, present since landing, never reviewed - an unreviewed default, against the
  no-baking-defaults rule). `{blank}..{blank}` iterates once with 0; `{blank}..3` iterates 0..3.
  Reproduced through the real run_staging: a byte-map rule over the staged node rows invented a
  `QB0` line for the input-only nodes (Q start/end blank), audited `ok`, nothing rendered. The
  choice - a blank end is (a) a located error (authors guard with `@if present(...)`) or (b) an
  EMPTY range - is a semantics decision; the code is left as is until it is made, then pinned.
- **holes, pinned** (the code held): a FILE rule naming a ROW template caught with ZERO matches (only
  the pre-check sees it); the `_db` rebuild after a PARTIALLY failed spawn; record load/save failures
  of other exception classes (a directory where the CSV should be; ENOSPC). Mutants Z1-Z4 (the
  refuter's MU1/MU2/MU3/MU10) killed -> **76/76** on the current code.
- **low-severity notes (outside the clause, recorded for later reviews):** the manual's own DIAG
  example sets neither `combined_FLD` nor `source_cell` (signals' key columns), so every spawn gets
  the SAME uid - a cascade's provenance cannot tell the triggering rows apart (for the
  generated-signals review); `{count(signalz)}` (a typo'd table in a data function) renders `0.0`
  silently (C-002 - a `@for` over it is a located error); `type: "{$type}"` copies a Python repr into
  a JSON column; a TIA pragma line `{ S7_Optimized_Access := 'TRUE' }` cannot be written as a literal
  (braces are holes - a located error, not silent; matters for SCL generation, P-012/P-013).

## The blank-range-end DECISION (user, 2026-09-25) — implemented

Asked after round 10, the user chose: **a BLANK range end (an empty cell) is an EMPTY range - no
iterations** (over "a located error" and "keep 0, document it"), matching what a blank count meant
in the original VBA Tempemplator. A literally MISSING end (`@for $i in ..3`) is a syntax slip, not
blank data - a located error. Clause, renderer chapter and the templates.yaml manual updated. Pinned:
blank..blank / a blank count / a blank start / whitespace -> nothing; `..3` -> located error; real
ends still iterate; and on the REAL fixture through run_staging the refuter's byte map now writes
exactly the real Q bytes (an empty line, no invented QB0, for every input-only node). Mutants: B1
(blank reads as 0 again) and B2 (a missing end treated as blank) - see the mutation line below.
The user also chose: continue refute rounds until holds/trivial, then verify.
Mutation check after the decision (scratch copy): **78/78 killed, all applied** (M11 re-anchored;
B1 blank-reads-0 and B2 missing-end-as-blank added).

## Refuter round 11 (2026-09-25, fresh context, at 1bac75c): REFUTED on EVIDENCE HOLES only — no code bug

HEAD behaved correctly on every probed input (blank/whitespace/NBSP/None ends, a real "0", shadowed
loop vars, inline vs block, the real-fixture byte map's `created`, BOM-headed CSVs...). The verdict
rested on four behavior-changing mutants that survived the linked tests - now pinned:
- the loop-column check at the RANGE ENDS (since a blank end = an empty range, a typo'd
  `{$r.Q_startBytee}` there would otherwise be a SILENT empty range - the decision made this check
  the only guard);
- a missing TAIL (`1..`, inline and block) - only `..3` was pinned;
- an inline `:` with an EMPTY body (the Python habit: body on the next lines);
- a loop var that did not exist before its loop is GONE after it (`{$i}` after the loop is missing,
  not the last value).
Plus, from its trivia: `after_300` passing the run-plan's hooks is pinned (the unfired-hook finding
rendered exactly twice), and a malformed end is now an error EVEN beside a blank one (every
non-blank end validated first - `abc..{blank}` was a silent empty range; consistent with the
decision). Housekeeping: the refuter left two stray logs OUTSIDE the repo (C:\Program Files\Git\
tmp_out_test_*.txt); its own delete was blocked by a safety check - reported to the user.
Mutation check after round 11 (scratch copy): **85/85 killed, all applied** (+ B3 blank-not-empty,
R11a-f: range ends unchecked, missing tail, inline empty body, loop var not popped, after_300
undeclared, blank winning over a malformed end).

## Refuter round 12 (2026-09-25, fresh context, at f63c0ff): REFUTED — 1 real bug, 1 hole; fixed

- **(medium) `@else if <pred>` silently became a bare `@else`** - `_line_word` reads only a directive
  line's first word, so the condition was dropped and the WRONG branch rendered: through the real
  run_staging a per-type rule gave 83 of 117 signals a `W_Manager(...)` call, audited `ok`, nothing
  rendered. `@else @use B` dropped the @use; `@elif` / `@elsif` (SCL's ELSIF habit) inside an UNTAKEN
  branch were never seen at all. Fix (the conservative one - no new grammar): directive GRAMMAR is
  checked wherever a line is rendered AND wherever a block is scanned - laziness is about VALUES,
  never grammar: an unknown directive, any text after `@else` / `@end`, a bare `@if` are located
  errors; the message says to nest an @if inside the @else. (A real `@elif` chain would be a new
  grammar feature - the user's call, not taken here.)
- **hole** the file TARGET's strict render was unpinned (`mode="empty"` or dropping `mode=` survived:
  a typo'd `gen/{$nmae}.txt` appended every row into `gen/.txt`, audited ok) - pinned, with nothing
  written. Whitespace around `in` in any amount pinned too.
- **trivial / outside the clause (noted)**: case-insensitivity of directives/keywords is unclaimed;
  a hole wrapped across two lines passes verbatim (visible); stale per-hook audit rows after a hook's
  last rule is removed, and reaction findings removed from validation_issues by the next phase's
  re-stage - variants of the recorded stale-log / re-stage boundaries; `@if $i = 1` in a range loop
  never matches because expr's `=` compares `"1"` with `"1.0"` as strings (C-002 - worth a C-002 look).
Mutation check after round 12 (scratch copy): **91/91 killed, all applied** (+ G1-G6: the scan and
the render skipping the grammar check, text after @else/@end allowed, a bare @if allowed, the target
rendered leniently, a single space after `in`).

## Refuter round 13 (2026-09-25, fresh context, at 6d7c01a): REFUTED — 2 real bugs, 1 hole; fixed

It first confirmed the round-12 grammar check rejects NOTHING legitimate (case, indentation incl.
NBSP, CRLF, trailing whitespace, the nested else-if idiom, @use chains, table/where/regex/slice heads).
- **(low-medium) nested STRUCTURE in untaken branches** - the scan treated `@else` as structure at
  depth 1 only: a forgotten inner `@end` got "block never closed" blaming the OUTER @if, the author
  appended `@end` at the bottom, and every row on the false side silently lost its @else line (the
  real fixture's 16 cabinets: `(16,16,ok)` instead of `(16,32,ok)`). Fix: `_scan_block` keeps a
  stack of open blocks - a misplaced / second @else is located at ANY depth, and a never-closed
  block is located at the INNERMOST open one (the author's real slip).
- **(latent, high impact) a ':' in a rendered file name** wrote into a HIDDEN NTFS alternate data
  stream (a 0-byte visible file, audited ok) - an IEC terminal `-X1:3` would trigger it. Fix: refused
  as rx_file_write before opening, nothing written.
- **hole** round 12's grammar pins were depth-1 only (a depth-1-only scan check, the @for head check
  and the bare-@use check each survived) - depth-2 slips pinned; the grammar check now also covers an
  inline @for body's directive (even with zero iterations) and a dangling `where` (was "every row").
- The file action's UTF-8 encoding pinned (non-ASCII text). Observed in passing: text mode writes the
  platform newline (CRLF on Windows) - unchanged, outside the clause.
- **outside the clause / trivial (noted)**: `{}` / `{ }` vanish (C-002's empty hole - matters for RTX
  C code, P-013); a numeric YAML template name (`07:`) loads as 7 (loud); case-insensitive directives
  unclaimed; the fixture join `'000'` vs `0` is the recorded C-002 string-equality note.
Mutation check after round 13 (scratch copy): **97/97 killed, all applied** - M10 RETIRED as
equivalent (the stack-based scan of the OUTERMOST block validates every nested structure at the
right absolute line first, so an inner scan can never raise - its base is unobservable); the
now-unreachable render-time checks (inline empty body, @for head, iterable shape) removed as dead
code (R11c re-anchored on the live grammar check; G1 re-anchored); + H1-H7: @else kind / second
@else at depth 1 only, never-closed at the outermost, a dangling where, an inline body unchecked,
the colon guard, the file encoding.

## Refuter round 14 (2026-09-25, fresh context, at 9b9104d): REFUTED on EVIDENCE HOLES only — no code bug

It found NO behavioral bug: a 20,000-template differential fuzz against a clean reference parser
(which DOES catch the surviving mutants) gave 0 divergences; round 13's scan rejects nothing
legitimate (4-level mixes, inline @for in blocks, @use in loops, tabs, uppercase); the colon guard
accepts drive, UNC, `\?\` and `\.\` absolute paths. The two holes, pinned:
- the TOP-level second @else lost its only pin when round 13 retired M10 (the nested case now dies
  in the outer scan first) - a pasted branch would silently drop the middle one; pinned at depth 1.
- an @else directly inside a top-level @for with ZERO iterations (a Jinja-style for/else) - the only
  pin ran one iteration, where the render-time error masked the scan check; pinned.
Closed from its minor list (cheap, consistent): an inline @for body cannot be a block directive
(`: @else` / `: @end` / `: @if c` were accepted when never rendered); a ':' anywhere in a RELATIVE
target is refused (`X:3.txt` read as drive X: - a drive-relative escape past the stream guard; the
absolute-path stream guard gained its own pin); `where` must be a whole word (`wherever` misread);
the manual warns that a PLAIN multi-line YAML value folds into one line.
Mutation check after round 14 (scratch copy): **102/102 killed, all applied** (+ I1-I5: the top-level
second @else, the @for scan read as an @if, inline block directives, the relative-target colon - its
pin now asserts WHICH check refused it (an unmapped X: fails to open anyway) - and `where` as a prefix).

## Refuter round 15 (2026-09-25, fresh context, at b7258b7): REFUTED on EVIDENCE HOLES only — no code bug

Round 14's changes hold on legitimate input (inline `: @use x` / nested inline @for incl. quoted or
regex colons, every relative target form, `WHERE` / tab / NBSP / `where not`). The holes, pinned
(its proposed pins adopted after review):
- **(medium) the E1 scope and the in-hook cascade as CONDITIONS, SOURCE MATCHING and ROW TEMPLATES
  see them** - only a fire-once TEXT template had been pinned: evaluating a condition without the
  scope, or without `_db` / `_params`, or a row template without `_db`, each silently matched 0 rows
  (a `lookup` filter, a `_params.rx.enable` gate, a `count(...)` guard over earlier spawns, a later
  rule's SOURCE matching the spawned rows) - audited ok. Pinned with exact audit tuples, spawned
  values and file bytes.
- **(low-medium) INDENTED directives** - every pin put directives at column 0, so "directives only at
  column 0" (the tempting fix for the recorded literal-@ hazard) survived: indented @use / @if /
  @else / @end inside a REGION would come out as raw text. Pinned with exact output (body lines keep
  their indentation - which also pins literal indentation).
- trivial, pinned anyway: the block-form half of the inline-body refusal (`: @for $j in 1..2`).
- **noted, unchanged**: a rooted target `/gen/x.txt` joins onto the output root's DRIVE (the `..`
  escape family - recorded hazard); a `>` folded YAML scalar collapses like a plain one; an
  extensionless target named `NUL` writes to the NUL device; `where(` without a space is now a
  located error (loud).
Mutation check after round 15 (scratch copy): **109/109 killed, all applied** (+ J1-J7: a condition
evaluated without its scope / without _db / without _params, a row template without _db,
directives at column 0 only, literal indentation dropped, the block-@for inline-body refusal).

## Refuter round 16 (2026-09-25, fresh context, at d47d420): TRIVIAL — C-024 VERIFIED

(A first attempt stalled on a >10-minute command - the full verify script now runs ten real-fixture
smokes - and returned no verdict; the retry ran the unit files and direct probes.) No C-024 clause
violation and no surviving mutant a realistic author would be hurt by. Checked and holding: the
record save paths, the both-or-neither attach, the uid dedupe, per-hook audit replacement, `hooks`
and index problems, Deferred / settle / the halted path, the in-hook cascade, the `_split_top` vs
tokenizer consistency (quotes, regexes, escapes), regex `{n}` quantifiers in predicates, the grammar,
laziness and the loop-column check. What it listed is outside the clause or low-impact:
- **C-002 surface -> P-015**: a regex `{n}` quantifier INSIDE a `{...}` hole is not recognised as a
  hole (visible garbage; a typo inside escapes the strict check); `{$idx:03}` (no type letter) on a
  text value right-pads (`"7"` -> `"700"`); an unknown named group in `regex_replace` leaks an
  IndexError (loud - relabelled rx_rule_crashed).
- **P-012 lint**: `@for $s in signals where $s.x = ...` silently matches 0 rows (the predicate sees
  the row's columns - a deliberate boundary; the builder should flag it).
- **noted**: a reactions.csv row starting with `#` is a LIVE rule (only fully blank lines are
  skipped; the shipped manual says CSV has no comment syntax); `body.split("\n")` vs `splitlines()`
  in `_template_lines` is unpinned (a `|` clip template would gain a blank line per match) - left
  unpinned deliberately: editing the linked tests now would re-stale this cleared refute.

**Verdict recorded: trivial. `zen verify C-024` -> verified** (the links resolve, the clean-tree run
at d47d420 is fresh and green incl. parity 0, the critic cleared it). Eleven refute rounds this
session (6-16): the code rounds found 26 real bugs, every one fixed and pinned; 109 mutants killed.

## P-012 phase A - literal braces `{{ }}` (2026-09-25)

User decision in the P-012 spec review: a literal brace is written DOUBLED (Python's format-string
rule) so a TIA pragma is writable in a text template (refuter round 10's note: `{ S7_Optimized_Access
:= 'TRUE' }` parsed as a hole). Built as ONE left-to-right scanner (`tempemplator._scan`: hole /
brace / lone / text runs); `render_text` renders each hole on its own, so a literal brace can never
pair up into a hole and no placeholder character can leak (a first placeholder-swap draft was
replaced for exactly that: `{ {{x}} }` would have formed a hole across the placeholders). The engine's
row-template values and file targets render through `render_text` (one brace rule wherever a
template renders). A lone brace - an extra `}`, an unclosed `{`, a brace inside a hole such as a regex
`{2}` quantifier (P-015 item 1, on the Tempemplator path) - is a located error, never copied.

Pins: `test_tempemplator.test_literal_braces` (pragma, hole inside a pragma, `{{{$x}}}` / `{{$x}}` /
`{$x}}}` / `}}{{` pairing, inline @for body, block branch, the loop-column check ignoring a doubled
literal, five lone-brace typos each located with its column); `test_chain_reactions.
test_doubled_braces_in_row_values_and_targets` (a JSON literal row value, a braced target + pragma
text, a lone brace in a row value / a target = rx_bad_template with nothing spawned / written).

Mutation check (scratchpad mutate_c024.py, BR1-BR12 + G4 re-anchored on `render_text`): 13/13 KILLED
- no doubled braces, a brace emitting both chars, a lone brace copied, the column check reading
literal runs, row values / target on the old render, leading / trailing text dropped, a brace
stepping one char, `start` not advanced, a zero-based column, `_render_line` bypassing the scanner.
The scanner's hole-vs-brace priority is NOT a mutant: a hole cannot contain `{`, so `{{` never
matches the hole shape at the same position (the redundant `ch == "{"` guard was removed for the
same reason).

## Refute round 17 (P-012 phase A, 2c1e755 / snapshot 25cf306) - HOLDS

The refuter ran on a `git archive` snapshot of 25cf306. Both linked files were green: 11/11 and 39/39.

What it confirmed:
- Scanner vs render: `render_text` hands expr.render exactly the `{body}` that the same `_HOLE` matched.
- Brace pairing matches Python `str.format` on every probe: `{{}}`, `{}}}`, `{{{{$x}}}}`, CR/LF, unicode.
- A lone brace is a located error everywhere: lines, blocks, inline bodies, range ends, `@use`'d templates, loops, row values and file targets.
- Pragmas and JSON row values work byte-exact through the REAL YAML loader and `engine.fire`.
- The three behaviour changes vs eb25136 are all the documented intent: stray braces are now loud, `{{$x}}` renders the literal text, and a doubled brace collapses.
- No shipped content was affected.
- The LAZY branch not reporting a lone brace is consistent with the clause: the brace rule is value-level, next to STRICT, and GRAMMAR is the directive list. A malformed hole in an untaken branch was never reported either.
- Six of its eight extra mutants were killed.

Low notes, taken into the next C-024 change (with P-012 phase B):
- (1) Mutant N4 survived: `_HOLE` `*` -> `+` turns `{}` into an error with all tests green.
  - Pin `render_text("{}")==""` and `render_text("{{}}")=="{}"`.
  - The P-012 lint should flag the silent `{}` and `{{$x}}`: an empty JSON object in a row value renders "".
- (2) Mutant N5 survived (the advice shows `'}'`): pin the exact message.
- (3) A failing file TARGET lost its context. Holes now render one by one, so the message shows `'{$nme}'`, not the target: prefix it with `target ...:`.
- (4) A multi-line row value's "column" is a character offset: report line + column.

## Refute round 18 (the phase-B carve-out, snapshot 775f3b7) - REFUTED: four test gaps, no code bug

The refuter diffed acf8ece against 775f3b7 over 60+ inputs. It compared findings, audit rows, spawned rows
(uids included), files written, every renderer message and the fired hook tuple. The only differences
were the three intended message changes. (c) `Table.stamped`, (d) `staged_database` and (e) the declared
hooks all hold.

The gaps, each pinned:
- **G1:** an UNFORESEEN exception at the carve-out's catch sites (a row value in `_spawned`, a file target in
  `_file_output`) still ends as rx_rule_crashed, but no test held it. Widening either catch to Exception
  survived.
  - Pinned by `test_the_backstop_holds_at_every_render_site`. It patches `render_text` to raise on its 2nd
    call (a row value) and its 1st call (a target), and asserts rx_rule_crashed, the true partial count and
    the spec added before it.
- **G2:** the per-spec add order (`list(_spawned(...))` survived). The entry-2 case now keeps its Database:
  labels == ["ok-D1"], and the audit shows `created` = 1.
- **G3:** line + column was only tested with the brace on the last line. Pinned: "l1\nl2 }x\nl3 {$name}"
  -> "line 2, column 4".
- **G4:** the target prefix only one way. A failing TEMPLATE with a fine target must carry no target prefix
  (exact detail).

Also fixed: a spawned field named `self` crashed `Table.add(**values)` (rx_rule_crashed); the C-025 refuter
found it. The fire now adds with `add_row(values)`, pinned in test_chain_reactions.

Mutation check after the carve-out: 121/121 KILLED. 109 applied on the first run; 12 whose anchors the
carve-out moved were re-expressed on the new code with the same intent (G4, R11f, N20, S5, Y5, H1, H5, I4,
J4, BR5, BR6, BR11), and all 12 were killed.

## Refute round 19 (snapshot 3622e07) - REFUTED: two test gaps, no code bug

Claims (a)-(c) hold on 34 identical fire() scenarios old (775f3b7) vs new, with an exception injected at
every step `_act` takes. Only the intended `self` fix differs. `stage(save=False)` opens nothing for
writing (audit hook); `stage(save=True)` writes byte-identical files.

The gaps, both pinned:
- **F1 (low):** every multi-line lone-brace pin put the brace on line 2, so the column taken from the FIRST
  newline (`find` for `rfind`) survived. Pinned: "l1\nl2\nl3 }x" -> "line 3, column 4".
- **F2 (medium):** the per-rule backstop was pinned with RuntimeError only, so `except RuntimeError` (and
  `except (RuntimeError, ExprError)`) survived every file. A TypeError would then have crashed the staging
  handler. Pinned: a TypeError, a custom Exception, a KeyError and a ValueError each end as
  rx_rule_crashed with the true partial count.

Mutation check: the 121 re-expressed after the `_act` extraction (N29 re-anchored onto `_act`'s template
check) plus round 19's F1 / F2a / F2b make 124/124 KILLED.

## The engine surface C-025 refute round 2 changed (before round 20)

C-025's second refute round found one engine-level gap and needed three builder doors:
- **The Windows-path guard (a behavior change).** A character a Windows path refuses (`<>"|?*`, a control
  character) in a rendered target failed at `open()` - rx_file_write "Invalid argument". `_file_output`
  now judges it BEFORE the write and names it ("'?' is not allowed in a Windows path"), so the builder's
  lint and preview can give the same refusal. Same finding type; the rows before the refused one are
  written, as before. Pinned: `test_windows_refused_path_characters_are_judged_before_the_write` - each of
  the seven characters, the file list and the audit's partial count. Other platforms: no guard (they accept
  these characters).
- **`_persist(..., save=True)`**: `save=False` attaches + replaces the audit + records the findings in
  memory and writes nothing - `settle_view(database, deferred)`, the builder's model of `settle`. The real
  `settle` / fire path passes the default.
- **`fire(..., write=False)`** - a DRY fire: every step but a file rule's write (counted as written). The
  builder's model of a settle; it gives the same Deferred as a real fire and writes nothing, an absolute
  target included (pinned: `test_a_dry_fire_is_the_fire_minus_the_write`).
- **`copy_database`** (the cascade's `_copy` per table) and **`WouldNotFire`** (a hook loader's "the run
  halts before this hook") - builder doors, no fire behavior.

The C-024 mutation runner: N23 / W2 re-anchored onto the moved code (the `save` branch; the parser's
`reads=` argument) with the same intent, and four mutants added for the new surface (the guard off, one
character short, no control characters, `_persist` never saving).

### The raw-FAIL guard for the reactions (C-025 round 2's implications check)

`run_staging` gated the after_300 fire with the host gate only (the EFFECTIVE severity), so a blocking
staging finding the operator downgraded let the reactions run - unlike every generation phase, which
refuses a raw FAIL (`run.has_blocking`). With no I/O sheet, the after_300 fire then saved the EMPTY staged
Database over signals.csv. Now a raw FAIL skips the settle and the after_300 fire: a WARN says the
reactions never run on it, the deferred before_300 firings are listed (not recorded). Pinned through the
real run-plan: a downgraded FAIL - staging on, nothing fired, the WARN; a downgraded no-I/O-sheet FAIL -
signals.csv and the audit byte-for-byte untouched, the before_300 firing listed.

Mutation check after these changes (scratchpad mutate_c024.py): **132/132 KILLED**. N23 / W2 were re-anchored
on the moved code (the `save` branch; the parser's `reads=` argument), and G1-G7 plus H1b were added: the
Windows guard off, one character short, and without control characters; the dry fire writing; the fire
ignoring `write`; `_persist` never saving; the run-plan's raw-FAIL guard; its listing. The full run killed
127/130. The 3 not applied (M6 / H1 / W7) had moved into the raw-FAIL guard's branch; they were
re-anchored and re-run on the final code with H1b + G7: 5/5.

## Refute round 20 (snapshot f8046bb) - REFUTED: three gaps, no bug in the landed code

What held: a differential against 3622e07 - 49 fire / settle scenarios (an exception injected at every
step, both hooks, the 310 append, ragged / unwritable records, index problems, unloadable files, re-fire,
the cascade): 44 identical, 5 differ only by the Windows guard's named message (NUL moves from
rx_rule_crashed to rx_file_write, as intended); 22 run-plan scenarios: 13 identical, 9 exactly the
raw-FAIL guard. `write=False` = the same Deferred, nothing written; `save=True` on every real path; the
parser change semantics-neutral over 1007 expressions x 9 observables; the Windows guard right for real
paths (drive colons, `\\?\`, UNC: no false positive).

- **G1 (MEDIUM, test gap): the raw-FAIL guard was pinned on the 300 leg only** - a guard skipped on 310 /
  320 survived the smoke and wiped signals.csv there. The no-I/O-sheet pin runs every leg now (None,
  310, 320), downgraded (skipped, the WARN, the listing) AND untreated (the halted path) - signals.csv,
  the audit and the rule's file untouched each time; the builder's stop on both legs.
- **G2 (LOW, test gap): the dry fire never met a refused target** - a guard moved after the makedirs
  survived C-024's files. The dry-fire pin adds `X:1.txt`, `rx/bad?.txt` and `r<x/ok.txt` (the dry
  Deferred = the real one, named, nothing written), and the Windows pin a refused DIRECTORY character
  (named, no directory made).
- **G3 (LOW, intent): the engine DARK warned** - a downgraded FAIL logged the reactions WARN with zero
  rules (as shipped). The WARN now comes only when reaction work is dropped (a deferred before_300
  firing, after_300 business - a rule, an index problem); pinned with an empty reactions.csv.

Noted, not changed: the guard scans the path before normalising (`sub/a<b/../x.txt` - Windows accepts
it, the guard refuses it; consistent across lint, preview and fire); a refused character in a UNC SHARE
name is left to the OS (no such share can exist); the RSLT line of a downgraded no-I/O-sheet run says
"staged 0 signals" (staging's report, outside C-024).

Mutation: the refuter's three survivors + the dark engine's WARN as mutants - 4/4 killed; every parser and
run-plan mutant re-run on the final code (the parser's reads grew) - 17/17 killed.

## Refute round 21 (snapshot d00884e) - REFUTED: one old gap (never silent / never a crash), fixed + pinned

What held: `_reactions_dropped` = "a reaction would have run" over 22 rule-index shapes on every leg; the
only run-plan difference from f8046bb is that WARN (174 of 220 cells identical, 30 the dark engine's WARN
gone, 16 per-run temp-path noise); the parser change semantics-neutral over 2,970 expressions.

- **F1 (LOW): text UTF-8 cannot store crashed the run.** A YAML `\uD83D\uDE00` escape pair decodes to two
  lone surrogates; nothing checked a rendered value or text before the write - the save raised
  (UnicodeEncodeError is a ValueError; `_persist` caught OSError only), the staging handler crashed (no
  RSLT, the spawns and findings lost), a file rule left a 0-byte file, and the dry fire said ok. Now a
  spawned value and a file text are checked before any add / write (rx_bad_template - the entry + field,
  or the template; nothing added or written; the preview and the dry fire agree), and the save's guard
  catches ValueError (rx_record_unwritable). Pinned: the engine (a row value, a file text dry = real, a
  save that raises ValueError) and the REAL run-plan (the emoji escape: RSLT, the finding rendered and
  recorded, signals.csv the staged rows).
- **N1:** the "a deferred before_300 firing" half of the WARN guard was unpinned - pinned (a before_300-only
  rule file, a downgraded FAIL: the WARN).
- **N2:** the rule loader dropped content silently - a headerless reactions.csv read its first rule AS the
  header; a line whose only content sat past the header's columns was skipped as blank. Now a header
  that names no rule columns is unreadable (rx_rules_unreadable) and an overflow-only line reaches the
  compiler (rx_bad_rule). Pinned.
- **N3 / C-025 round 4:** refused characters in the drive / share position, a non-letter drive and an
  extended drive-relative path are refused before the write, named (see C-025's round 4).

Mutation check: every engine, loader, parser and run-plan mutant on the final code (8 re-anchored, 8 new: the unencodable value / text, the save's ValueError, the header check, the overflow line, the drive rules) - **83/83 killed**.


## Refute round 22 (snapshot 0156f36) - REFUTED: 3 gaps + 4 notes, all fixed + pinned

What held: the loader's header shapes (another order, extra columns, a BOM, quoted headers, CRLF, header-only,
empty and blank-only files; a changed case / spaced names / a `;` delimiter / a missing rule column now read
rx_rules_unreadable - loud, as the clause says); the shipped Siemens reactions.csv loads as []; 48/48 fire
scenarios and 6/8 run-plan scenarios identical to d00884e (the 2: the intended "'1:' is not a drive"); a lone
surrogate in a TARGET is fine at the OS; NUL / CR values round-trip the CSV.

- **F-A (MEDIUM): a finding QUOTING text UTF-8 cannot store still crashed the run.** Round 21 checked spawned
  values and file texts only; a finding's detail copied decoded text raw - a duplicated templates.yaml name
  (ruamel's message quotes the value), a malformed /regex/ in a line, an @use cycle joining template names:
  recording it crashed at the finding's uid (UTF-8) - at the settle on a database-less hook, at the render seam
  on before_300. Every engine finding (`_f`) carries its detail and location escaped now (`\ud83d` -
  backslashreplace), and so does a preview's problem. Pinned: the engine (the three channels on both hooks:
  recorded, escaped; the preview = the fire) and the REAL run-plan (a duplicated name's escape on after_300
  and before_300: RSLT, rx_templates_unreadable rendered + recorded escaped).
- **F-B (MEDIUM): a lone surrogate in a row template's FIELD NAME emptied the table's CSV.** Field names were
  never judged: the row was added, the save failed on the CSV header - after `write_csv` had opened the file
  ("w" truncates) - signals.csv 0 bytes while RSLT reported 293 rows, the finding blaming "a file held open";
  round 21's ValueError catch had turned a crash into a quiet wipe. Now: the field name is the row template's
  own problem (`_row_template_problem` - rx_bad_template naming the entry and the field, before any matching;
  nothing added; the lint and the preview say the same); a save renders EVERY table before it writes any
  (`Table.csv_bytes` - a value that cannot be written raises with every file intact, [[C-001]]); and
  rx_record_unwritable names its cause. Pinned: the engine, `Database.save` (the table before a failing one is
  not written either), `write_csv` (the previous file intact - test_ssot_table), the cause wording, and the
  REAL run-plan on both after_300 legs (signals.csv byte for byte as staged, the audit on disk).
- **F-C (LOW): a blank first line got past the header check** - DictReader read it as an empty header: every
  rule lost to three unnamed rx_bad_rule findings. The header is the first non-blank line now. Pinned.
- **The notes, fixed too (C-025 round 5 found their builder half):** the fire refused `\\?/C:/...` (Windows
  writes it) - a device prefix is any Win32 spelling now (two separators, `.` / `?`, a separator); `rx/NUL`
  wrote into the null device (audited ok) and, on older Windows, `CON` / `COM1` / `NUL.txt` do the same - a
  device name is refused; Windows drops a trailing '.' / ' ' (`rx/a.` appended to `rx/a`; `rx /a.txt` failed
  after making `rx`) - refused; a rooted target (`/rx/a.txt`) was joined onto the drive's ROOT - refused
  before the join. Refused before the write too: a `\\?\` path's '/', '.' / '..' / empty name (Win32 takes it
  as written), a device path naming no drive / share / volume (`\\.\pipe\x`) or whose '..' climbs above its
  drive, a target naming no file. Each shape was probed against the OS first (41 shapes: the guard refuses
  exactly what Windows refuses or writes elsewhere, and every spelling Windows normalizes is written by its
  own name). The guard judges the TARGET as written - a relative one's own names, never the output root's
  (Windows 11 allows a folder `aux.files`; judging the joined path refused every target under it - found
  while fixing, pinned). Pinned: the fire (21 refused shapes, dry = real, nothing written or made; 9 accepted
  spellings written by their own names).

Mutation check: the path, save, loader, record and finding-text mutants on the final code - 2 re-anchored (G2, D2), 18 new (the finding text, the preview's problem, the field name, the save rendering every table first, `write_csv`, the cause, the blank header line, the eleven path guards - the output root's names included) and 19 earlier ones re-run over the rewritten guard - **39/39 killed**.
