# P-001 — evidence / lab notebook

## 2026-07-16 — the approved PL5 execution plan (architecture spike + design review)

**Plan file (user-approved after 5 review rounds):**
`C:\Users\bogdan.dragoi\.claude\plans\another-big-step-now-curried-pnueli.md`

Produced via: 3-agent codebase exploration (project-creation/system-type seam, Siemens-vs-generic
architecture map A/B/C, GUI phase wiring + OP4 output contract) + a Plan-agent design + the user's
corrections. Key findings recorded for posterity:

- `PROJECT_TYPES` in `Pipeline4App/pipeline4/project/project.py:33` already declared the RTX roadmap
  types, persisted to project meta, consumed by NOTHING — the gap the plugin architecture fills.
- The 10 hardest Siemens couplings (plc_binding string, emit tables, config output tree, F_DB dual
  enforcement, builders fusion, CreationInfo contract, %I/%Q+Profinet, PLCTags monolith, GUI single
  pipeline, blended config vocabulary) — each has a decided resolution in the plan.
- `core/expr` has ZERO TIA leakage (quoting lives in interface_scl) — it is the reusable kernel.
- User corrections during review: the I/O address notation differs per system → config regex
  ([[P-010]]); config_project renamed/regrouped (shared vs per-system tiers); `chain_reactions`
  STRENGTHENED into the kernel rule engine ([[P-009]]) — fire_when/condition/action, one of the
  app's key features; file action appends by default; Tempemplator (the user's VBA tool at
  `…\FUH_DHL\5 Misc\1 Tools\Tempemplator`) integrated as the text renderer; a context-aware
  template-builder pilot ([[P-012]]).

**Decisions locked** (user, 2026-07-16): taxonomy `PLC_Based/SiemensS7/Safety` +
`IPC_Based/Intervalzero_RTX/{Sorter,Induction,Plant}`; system ids renamed to taxonomy ids;
multi-system v1 = active-system selector; scope = full arc incl. RTX stubs (7 steps).

**Acceptance instrument:** the committed byte-parity oracle `Pipeline5App/scripts/parity_vs_pl4.py`
(frozen PL4 @ 16ef215 as reference generator) — 0 diffs on every BuilderData surface + SSOT table;
the only sanctioned delta is the `#` generator-comment line (normalized in the oracle).

## 2026-07-16 — steps 1–2 landed

- **Step 1** (`65b744e`): Pipeline5App shell — `systems/system_contract.py` (System, Capabilities,
  SymbolFormatter, OutputLayout, instance-based BuilderRegistry, PhaseContext) + `systems/catalog.py`
  (explicit registry; availability-from-membership; 4 PLANNED taxonomy-id rows) + 6 plugin-seam tests.
- **Step 2**: the WHOLE pipeline4 package moved to `pipeline5` via a deterministic migration script
  (scratchpad `migrate_step2.py`): 194 files, 130 renamed per the approved map; imports rewritten with
  `as`-aliasing so function bodies stayed byte-untouched; dotted + slash-path string forms and the
  `src://` guide links rewritten; `config.py` → `core/config/__init__.py` (the 4-way split lands with
  the resolver). Hand-fixes required (4): APP_ROOT one-level-deeper for the config package; the single
  parenthesized multi-line import (`fillout/document_fill.py`); `__version__ = "5.0"` on the kept
  scaffold `__init__`; the requirements.txt header comment. **Verification: 61/61 test files PASS**
  (the full former-PL4 suite, renamed, + test_systems_contract) run per-file with the venv python.
  Siemens B/C modules came along under their PL4-ish names in `domain/` — they relocate + rename under
  `systems/plc_based/siemens_s7/` at step 4 ("still-in-place" per the plan's step 3).

## 2026-07-16 — the step-2 REGROUP pass (the user's regrouping grant, relayed from a /btw session)

Grant: moving functions between files / merging is allowed; navigability is the criterion; the calls
are Claude's (memory: regrouping-allowed-navigability). Landed as separate green-proven commits after
the mechanical move, per the agreed discipline:
- **config split**: `core/config/__init__.py` (814 lines, four jobs) → `paths.py` (roots + project
  state + every *_dir) · `params.py` (YAML: project/app-UI/document params + sheet resolution) ·
  `loaders.py` (config-CSV readers + DTD); `__init__` re-exports the full surface + signpost.
  One test's monkeypatch point followed the code (test_generation_params → patches config.params).
- **content-based renames**: `gui/_filexy.py`→`filexy_shim.py` (dependency shim earns separation);
  `gui/excel.py`→`excel_goto.py` (it is a COM jump-to-cell helper, not a grid);
  `changes/run.py`→`report_runner.py` (mirrors validation_runner); log tee stem `pl4_log`→`pl5_log`.
- **signpost `__init__` docstrings** on core/ domain/ gui/ io/ project/ ("where to look", suffix
  conventions, the TRANSITIONAL Siemens list pointing at step 4).
Verification: the full 61-file suite green after the split AND after the renames (per-file, venv).
