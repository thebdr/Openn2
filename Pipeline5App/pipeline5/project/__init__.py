"""Folder-based Project Manager for PL4 (GUI M6): open/close/new a project + the persisted
recent/last-opened state. A project ROOT is a folder carrying `config_project/project_params.yaml`
(+ its Database/ and Output/); `config.use_project(root)` points every loader at it."""
