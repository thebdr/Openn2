"""The composition root - the only part allowed to know everything.

  main.py   the entrypoint behind `python -m pipeline5` and launch_gui.py
            (the phase registry + system wiring move here from the workbench at steps 3/5)
"""
