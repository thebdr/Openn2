"""The PL4 operator GUI (Tkinter). Wired in early - against a real window - so integration problems
surface as phases are ported, instead of after a big headless build. Launch: `python launch_gui.py`."""
