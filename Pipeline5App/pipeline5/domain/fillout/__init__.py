"""Phase 200 - Documents Fill Out (ph200): compute the classified columns (script_type / index /
diag_cabinet / diag_bit) for a RAW I/O List. CSV-rule-driven (see `PH200_SPEC.md`); built one sub-phase
at a time. 210 (script_type classification) lives in `classify.py`; `reader.py` reads the raw rows."""
