"""SiemensS7 - toolchain level: the shared TIA code (symbols, output layout, emitters, hardware).

Populated by the step-4 extraction: symbols, output_layout, the emitters (globaldb_xml, fc_xml,
scl, interface scl/xlsx, plctags workbook, creation_info_csv), template_scanner, profinet
hardware + csv export. The safety/ system package registers the SYSTEM descriptor.
"""
