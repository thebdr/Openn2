"""Intervalzero_RTX - toolchain level: the code shared by the Sorter/Induction/Plant systems
(rtx symbols, output layout, the shared i/o tag-table format, cPCI hardware configuration).

Scaffolded at migration step 7; emitters land when the RTX delivery formats are specified.
"""
