"""Datablocks - the DB registry evaluated over signals (phase 520).

  generator.py   the registry evaluator -> db tables + the signal write-back (plc_binding et al.)
  templates.py   member rendering + the for_each iteration DSL
  _siemens_s7/   quarantine: the GlobalDB XML emitter (step 4 -> systems)
"""
