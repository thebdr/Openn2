"""Interfaces - machine interfaces built from IOC signals (phase 400).

  builder.py      the interfaces + interface_elements tables (mirror set, byte allocation)
  _siemens_s7/    quarantine: the IF_*.xlsx projection + the SCL emitter (step 4 -> systems)
"""
